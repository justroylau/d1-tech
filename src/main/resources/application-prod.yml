server:
  port: 8080

mybatis: 
  config-locations: classpath:mybatis/mybatis-config.xml  
  mapper-locations: classpath:mybatis/mapper/*.xml  
  type-aliases-package: com.liang.tech.pojo
spring: 
  datasource: 
    driver-class-name: com.mysql.jdbc.Driver    
    url: jdbc:mysql://localhost:3306/tech?useUnicode=true&characterEncoding=utf8
    username: root
    password: nopassword
  http: 
    multipart: 
      maxFileSize: 100Mb
      maxRequestSize: 1000Mb

logging:
  level:
     com.liang.tech.mapper : debug
     

     
